# =============================================================================
# config.py — Глобальная конфигурация «Цифровой коридор»
# =============================================================================

# ---------------------------------------------------------------------------
# КАМЕРА
# ---------------------------------------------------------------------------
CAMERA_INDEX = 0          # Индекс камеры (0 = первая доступная)
FRAME_WIDTH  = 1280       # Ширина кадра (если камера поддерживает)
FRAME_HEIGHT = 720        # Высота кадра
TARGET_FPS   = 30         # Желаемый FPS

# ---------------------------------------------------------------------------
# MEDIAPIPE HANDS — настройки для дальней/тёмной камеры
# ---------------------------------------------------------------------------
MP_MAX_HANDS            = 2      # Максимум рук одновременно
MP_DETECTION_CONFIDENCE = 0.55   # Нижний порог детекции (ниже = лучше видит издалека)
MP_TRACKING_CONFIDENCE  = 0.50   # Порог трекинга между кадрами
MP_MODEL_COMPLEXITY     = 1      # 0 = быстрая/лёгкая, 1 = точная (рекомендуем для дальних рук)

# ---------------------------------------------------------------------------
# ONE EURO FILTER — параметры сглаживания для рисования
# ---------------------------------------------------------------------------
ONE_EURO_MIN_CUTOFF  = 1.0   # Минимальная частота среза (Гц). Чем ниже – плавнее
ONE_EURO_BETA        = 0.05  # Коэффициент скорости. Чем выше – быстрее реакция
ONE_EURO_D_CUTOFF    = 1.0   # Частота среза производной

# ---------------------------------------------------------------------------
# EMA-ФИЛЬТР — для сглаживания общего положения руки
# ---------------------------------------------------------------------------
EMA_ALPHA = 0.35    # 0..1: чем ниже, тем плавнее (и инертнее)

# ---------------------------------------------------------------------------
# ТАЙМЕРЫ И ЖЕСТЫ
# ---------------------------------------------------------------------------
HOVER_CONFIRM_SECONDS  = 2.0   # Время удержания пальца над кнопкой для "нажатия"
IDLE_TIMEOUT_SECONDS   = 60    # Секунд без рук → сброс в меню
SWIPE_MIN_SPEED        = 0.25  # Минимальная скорость свайпа (доля ширины в секунду)
SWIPE_HISTORY_FRAMES   = 10    # Сколько кадров хранить для определения свайпа
FIST_FRAMES_THRESHOLD  = 5     # Кадров «кулак» подряд чтобы засчитать очистку

# Жест «две руки в кадре» → возврат в меню
BOTH_HANDS_BACK_FRAMES = 20    # Кадров с двумя открытыми ладонями для возврата

# ---------------------------------------------------------------------------
# UI — ЦВЕТА (BGR для OpenCV)
# ---------------------------------------------------------------------------
COLOR_WHITE   = (255, 255, 255)
COLOR_BLACK   = (0,   0,   0  )
COLOR_ACCENT  = (0,   200, 255)   # Основной акцент — жёлто-голубой
COLOR_ACCENT2 = (180, 0,   255)   # Вторичный — фиолетовый
COLOR_GREEN   = (0,   220, 80 )
COLOR_RED     = (0,   60,  255)
COLOR_ORANGE  = (0,   140, 255)
COLOR_GRAY    = (80,  80,  80 )
COLOR_DARK    = (20,  20,  30 )   # Тёмный фон оверлея

# Палитра кисти в рисовалке (BGR)
BRUSH_COLORS = [
    (0,   0,   255),   # Красный
    (0,   140, 255),   # Оранжевый
    (0,   255, 255),   # Жёлтый
    (0,   255, 0  ),   # Зелёный
    (255, 0,   0  ),   # Синий
    (255, 0,   180),   # Фиолетовый
    (255, 255, 255),   # Белый
    (0,   0,   0  ),   # Чёрный
]

# ---------------------------------------------------------------------------
# UI — РАЗМЕРЫ
# ---------------------------------------------------------------------------
BUTTON_WIDTH    = 320   # Ширина кнопки меню (px)
BUTTON_HEIGHT   = 130   # Высота кнопки меню (px)
BUTTON_RADIUS   = 20    # Скругление углов кнопки
TIMER_RADIUS    = 30    # Радиус кружка-таймера рядом с кнопкой
HUD_HEIGHT      = 56    # Высота верхней HUD-полосы
PREVIEW_SIZE    = 200   # Размер окошка превью скелета руки
PREVIEW_MARGIN  = 12    # Отступ превью от края экрана

# ---------------------------------------------------------------------------
# РЕЖИМЫ (константы для FSM)
# ---------------------------------------------------------------------------
MODE_MENU    = "menu"
MODE_DRAWING = "drawing"
MODE_SLIDES  = "slides"
MODE_GAMES   = "games"

# Заголовки режимов для HUD
MODE_LABELS = {
    MODE_MENU:    "Главное меню",
    MODE_DRAWING: "Режим рисования",
    MODE_SLIDES:  "Слайды",
    MODE_GAMES:   "Игры",
}

# ---------------------------------------------------------------------------
# ШРИФТЫ
# ---------------------------------------------------------------------------
import cv2
FONT_MAIN  = cv2.FONT_HERSHEY_DUPLEX
FONT_BOLD  = cv2.FONT_HERSHEY_SIMPLEX
FONT_SMALL = cv2.FONT_HERSHEY_PLAIN

# ---------------------------------------------------------------------------
# ПУТИ
# ---------------------------------------------------------------------------
import os
BASE_DIR    = os.path.dirname(os.path.abspath(__file__))
ASSETS_DIR  = os.path.join(BASE_DIR, "assets")
SLIDES_DIR  = os.path.join(ASSETS_DIR, "slides")
