# =============================================================================
# requirements.txt — Зависимости «Цифровой коридор»
# =============================================================================
# Установка: pip install -r requirements.txt

# --- Видео и компьютерное зрение ---
opencv-python>=4.8.0        # Захват камеры, рисование, окно

# --- Трекинг рук ---
mediapipe>=0.10.0           # MediaPipe Hands (21-точечный трекинг)

# --- Математика ---
numpy>=1.24.0               # Матричные операции, массивы

# --- Эмуляция клавиш ---
pynput>=1.7.6               # Нажатия клавиш для управления играми

# --- Фокус окна игры ---
pygetwindow>=0.0.9          # Поиск/активация окна по заголовку (Windows)

# --- Изображения ---
Pillow>=10.0.0

# --- PDF поддержка (для режима слайдов) ---
pymupdf>=1.23.0          # pip install pymupdf  (импортируется как 'fitz')

# --- PPTX поддержка (опционально, для .pptx слайдов) ---
python-pptx>=0.6.21      # pip install python-pptx
